# PR Merge — alias

Alias for `/merge`. See `.claude/commands/merge.md` for the full workflow and `.agent/skills/pr-merge/SKILL.md` for the skill contract.

Triggered when the user types `/pr-merge`. Behavior is identical to `/merge`.
