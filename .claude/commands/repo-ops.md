# Repo Ops — alias

Alias for `/repo`. See `.claude/commands/repo.md` for the full workflow and `.agent/skills/repo-ops/SKILL.md` for the skill contract.

Triggered when the user types `/repo-ops`. Behavior is identical to `/repo`.
